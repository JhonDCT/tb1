#include "ordenamiento.h"

template <typename T>
class HeapSort : public Ordenamiento<T>
{
    void ordenar(Lista<T> datos, cmp : (T, T)->int) {
        cout << "Ordenando con HeapSort" << endl;
    }
};